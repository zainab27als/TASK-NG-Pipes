import { LastLoginEmojiPipe } from './last-login-emoji.pipe';

describe('LastLoginEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new LastLoginEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
